//importing components
import NavBar from '../NavBar';
//header
const Header = () => {
  return (
    <header>
      <div className="container">
        <NavBar />
      </div>
    </header>
  );
};

export default Header;
